#pragma once
// Открывает шторку быстрых настроек (Brightness/WiFi/Bluetooth/Flashlight/Sound).
// Блокирующий вызов — крутится в своём цикле, выходит по удержанию A+B (>=500мс).
// Вызывать по долгому удержанию кнопки B (>=600мс) с главного экрана меню.
void k85_quick_settings_open(void);
