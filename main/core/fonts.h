#pragma once

#define K85_FONT_COUNT 5

// Названия для отображения в Settings.
extern const char *k85_font_names[K85_FONT_COUNT];

// Применяет шрифт idx (0..K85_FONT_COUNT-1) к дисплею немедленно.
// ВНИМАНИЕ: центрирование текста в большинстве экранов проекта считается
// вручную через strlen(text)*6 (ширина символа дефолтного битмап-шрифта).
// При выборе непропорциональных/других шрифтов центрирование на части
// экранов может визуально съехать - это известный компромисс, не баг.
void k85_apply_font(int idx);
