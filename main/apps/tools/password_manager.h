#pragma once
// Офлайн-менеджер паролей: мастер-пароль защищает список записей
// (сайт/имя, логин, пароль), сами пароли хранятся зашифрованными AES-256-CBC
// в конфиге устройства. Мастер-пароль не хранится - только его PBKDF2-
// верификатор; ключ шифрования держится в RAM только пока инструмент открыт.
void k85_run_password_manager(void);
