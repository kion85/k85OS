#pragma once
// Определитель ориентации устройства "какой стороной вверх" (по аналогии с
// игральной костью) - по доминирующей оси акселерометра относительно
// гравитации. Магнитометра на StickS3 нет, поэтому это НЕ компас (не знает,
// где север) - только "какая из 6 граней сейчас направлена вверх".
void k85_run_orientation(void);
